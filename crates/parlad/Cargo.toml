[package]
name = "parlad"
description = "parla daemon: PipeWire capture, Silero VAD, whisper.cpp ASR, PTT hotkeys, router"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "parlad"
path = "src/main.rs"

[dependencies]
parla-grammar = { path = "../grammar" }
desktopd = { path = "../desktopd" }
parla-flow = { path = "../flow" }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
tokio = { workspace = true }
zbus = { workspace = true }
whisper-rs = { version = "0.16", features = ["cuda", "tracing_backend"] }
# Only for the ggml backend registry, to tell whether the GPU came up.
whisper-rs-sys = "0.15"
llama-cpp-2 = { version = "0.1.156", features = ["cuda"] }
cpal = "0.15"
futures-util = "0.3.34"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1"
libc = "0.2"
